{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "node -e \"process.env.RENDER && process.exit(0)\" || nuxt prepare",
    "start": "node .output/server/index.mjs"
  },
  "dependencies": {
    "ace-builds": "^1.37.4",
    "nuxt": "^3.15.1",
    "vue": "^3.5.12"
  },
  "devDependencies": {
    "@types/node": "^20.17.5",
    "typescript": "^5.6.3"
  },
  "imports": {
    "#internal/nuxt/paths": "./node_modules/nuxt/dist/core/runtime/nitro/paths"
  },
  "engines": {
    "node": ">=20.10.0"
  }
}