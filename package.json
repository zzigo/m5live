{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "NODE_OPTIONS='--max-old-space-size=4096' nuxt build --no-prepare",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "start": "node .output/server/index.mjs",
    "github-build": "NODE_OPTIONS='--max-old-space-size=4096' nuxt build --no-prepare && nuxt generate"
  },
  "dependencies": {
    "ace-builds": "^1.37.4",
    "nuxt": "^3.15.1",
    "vue": "^3.5.12"
  },
  "devDependencies": {
    "@types/node": "^20.17.5",
    "typescript": "^5.6.3"
  },
  "imports": {
    "#internal/nuxt/paths": "./node_modules/nuxt/dist/core/runtime/nitro/paths"
  }
}